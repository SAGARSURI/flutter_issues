import 'package:flutter/material.dart';

class FlutterIssuesScreen extends StatelessWidget {
  const FlutterIssuesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flutter Issues'),
      ),
      body: Container(),
    );
  }
}
